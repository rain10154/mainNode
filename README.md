host:
nodes=>{mac}->{
                   'time':'',
                   'ip':'',
                   'port':''
              }

flow:
flow=>0:{port}->{f}


user:
user=>0:{port}->{
                    'd':'',
                    'p':''
                }

sudo pip install redis
sudo pip install Flask
sudo pip install request
sudo pip install jwt
